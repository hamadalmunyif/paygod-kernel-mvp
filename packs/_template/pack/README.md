# Pack Template (paygod/v1)

This folder is the canonical starting point for creating a new Pack.

## Required steps
1) Copy this folder into the desired location (e.g. packs/core/<name>/)
2) Edit pack.yaml metadata and rules
3) Run: python tools/pack_validate.py
4) Open a PR (Pack Contract Gate must pass)
